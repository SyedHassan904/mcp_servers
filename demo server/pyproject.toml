[project]
name = "demo-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Syed Hassan", email = "syedhassanshah154@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=4.0.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
demo-server = "demo_server:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
